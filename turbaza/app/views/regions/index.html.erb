<div id="top"></div>

<!-- Fixed navbar -->
<div class="navbar navbar-default navbar-fixed-top" role="navigation">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="<%= root_path %>">Мир турбаз</a>
    </div>
    <div class="navbar-collapse collapse">
      <ul class="nav navbar-nav">
        <li><a href="<%= root_path %>">Главная</a></li>
        <li><a href="<%= countries_path %>">Страны</a></li>
        <li class="active"><a href="<%= regions_path %>">Регионы</a></li>
        <li><%= link_to 'Города', cities_path %></li>
      </ul>
    </div><!--/.nav-collapse -->
  </div>
</div>

<div class="container">
  <div class="page-header" style="text-align: center">
    <h1>Регионы</h1>
  </div>

  <div class="row">
    <div class="col-md-2">
      <%= render 'form' %>
    </div>
    <div class="col-md-10">
      <table class="table table-bordered">
        <thead>
        <tr>
          <th>Название</th>
          <th>Страна</th>
          <th></th>
          <th></th>
        </tr>
        </thead>
        <tbody>
            <% @regions.each do |item| %>
            <tr>
              <td><%= item.name %></td>
              <td><%= item.country.name %></td>
              <td><%= link_to 'Edit', edit_region_path(item), :class => 'btn btn-info' %></td>
              <td><%= link_to 'Destroy', item, method: :delete, data: { confirm: 'Are you sure?' },
                              :class => 'btn btn-danger' %></td>
            </tr>
            <% end %>
        </tbody>
      </table>
    </div>
  </div>

  <div class="footer">
    <p>© Company 2013</p>
  </div>

</div> <!-- /container -->

<!-- go to top -->
<a href="#" class="go-top"><span class="glyphicon glyphicon-chevron-up"></span></a>
